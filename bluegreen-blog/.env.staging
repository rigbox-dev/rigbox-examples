# DEMO staging token for the bluegreen sibling:
#   rig deploy --stage staging --bluegreen v2
# Distinct from production so a staging cut-over can't publish with the prod
# token. Replace for real use (real secrets → `.env.local` / your shell).
ADMIN_TOKEN=staging-demo-admin-token
