# DEMO values, loaded by `rig deploy --stage production`.
# Replace for real use — put real secrets in `.env.local` (gitignored) or your
# shell, both of which override this file.
WEBHOOK_HMAC_KEY=prod-demo-hmac-key-0123456789abcdef
